#include <iostream>

using namespace std;

int N;
int team[20][20];

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);

  cin >> N;
  for (int i = 0; i < N; i++) {
    for (int j = 0; j < N; j++) {
      cin >> team[20][20];
    }
  }

  return 0;
}